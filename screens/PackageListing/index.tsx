import React from 'react';
import PackageListing from '../../components/PackageListing';

const PackageListingScreen: React.FC = () => {
    return <PackageListing />;
};

export default PackageListingScreen;
